name: Test

on:
  push:
    branches: [main, release, antistasi-main]
  pull_request:


jobs:
  backend-build:
    name: Build backend 🏗️
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: adopt